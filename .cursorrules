# SoybeanAdmin NestJS 低代码平台 - Cursor Rules

## 项目概述
这是一个基于 NestJS + Vue3 + Amis 的企业级低代码开发平台，采用 Monorepo 架构，包含5个核心服务：frontend、lowcode-designer、backend、lowcode-platform-backend、amis-backend。

## 技术栈规范

### 前端技术栈
- **框架**: Vue 3.5.16 (Composition API)
- **构建工具**: Vite 6.3.5
- **语言**: TypeScript 5.8.3
- **状态管理**: Pinia 3.0.3
- **UI组件**: Naive UI 2.41.1 + UnoCSS
- **图表**: ECharts 5.6.0
- **可视化**: AntV X6 2.18.1
- **低代码**: Amis 6.11.0
- **国际化**: Vue I18n 11.1.5

### 后端技术栈
- **框架**: NestJS 11.0.12
- **语言**: TypeScript 5.8.2
- **Web服务器**: Fastify 5.2.2
- **ORM**: Prisma 6.5.0
- **数据库**: PostgreSQL 16.3 (多Schema设计)
- **缓存**: Redis 7.2.0
- **认证**: JWT + Passport + Casbin
- **文档**: Swagger/OpenAPI
- **测试**: Jest 29.7.0

## 项目结构规范

### Monorepo 布局
```
soybean-admin-nestjs/
├── frontend/                    # Vue3 前端管理系统 (端口: 9527)
├── lowcode-designer/           # Vue3 低代码设计器 (端口: 9555)
├── backend/                    # NestJS 主后端服务 (端口: 9528)
├── lowcode-platform-backend/  # NestJS 低代码平台 (端口: 3002)
├── amis-lowcode-backend/       # NestJS 业务运行时 (端口: 9522)
├── shared/                     # 跨服务共享模块
├── deploy/                     # 数据库初始化脚本
├── scripts/                    # 自动化脚本
└── .qodo/                     # 项目文档
```

### 服务职责分工
- **frontend**: 企业管理系统前端界面，用户权限管理
- **lowcode-designer**: 低代码可视化设计器，实体关系设计
- **backend**: 主业务后端服务，用户认证、权限管理、系统管理
- **lowcode-platform-backend**: 低代码平台核心引擎，实体建模、代码生成
- **amis-backend**: 生成代码运行时环境，动态API服务

## 数据库架构规范

### 多Schema设计
```sql
-- 数据库: soybean-admin-nest-backend
├── backend schema    # 主业务系统数据 (用户、角色、权限、审计日志)
├── lowcode schema   # 低代码平台数据 (项目、实体、API配置、代码生成)
└── amis schema      # 动态生成业务数据 (演示数据和页面模板)
```

### 核心表结构
- **用户权限**: sys_user, sys_role, sys_menu, casbin_rule
- **多租户**: enterprise, tenant, app_space, user_group
- **审计日志**: sys_login_log, sys_operation_log, sys_tokens
- **低代码**: lowcode_projects, lowcode_entities, lowcode_fields, lowcode_relations
- **API管理**: lowcode_api_configs, lowcode_apis, lowcode_queries
- **代码生成**: lowcode_code_templates, lowcode_codegen_tasks

## 代码规范

### TypeScript 规范
- 使用严格模式 TypeScript
- 所有函数和类必须添加中文注释
- 使用接口定义数据结构
- 避免使用 any 类型，优先使用泛型
- 使用枚举定义常量

### NestJS 规范
- 遵循 DDD (领域驱动设计) 分层架构
- 使用 CQRS (命令查询职责分离) 模式
- 控制器只处理HTTP请求，业务逻辑放在服务层
- 使用装饰器进行参数验证和权限控制
- 统一使用 Prisma 进行数据库操作

### Vue3 规范
- 优先使用 Composition API
- 使用 `<script setup>` 语法
- 组件名使用 PascalCase
- Props 和 Emits 必须定义类型
- 使用 Pinia 进行状态管理

### 文件命名规范
- 组件文件: PascalCase (如: UserManagement.vue)
- 服务文件: kebab-case (如: user-service.ts)
- 工具文件: camelCase (如: formatUtils.ts)
- 常量文件: UPPER_SNAKE_CASE (如: API_CONSTANTS.ts)

## API 设计规范

### 响应格式
```typescript
// 统一响应格式
interface ApiResponse<T = any> {
  status: number;        // 状态码
  msg: string;           // 消息
  data: T;               // 数据
}

// 分页响应格式 (Amis 兼容)
interface PaginatedResponse<T> {
  status: number;
  msg: string;
  data: {
    items: T[];          // 数据列表
    total: number;       // 总数
    page: number;        // 当前页
    perPage: number;     // 每页数量
  };
}
```

### 错误处理
- 使用统一的错误码
- 错误信息必须中文化
- 记录详细的错误日志
- 敏感信息不能暴露给前端

### 权限控制
- 使用 Casbin 进行权限管理
- 所有API必须进行权限验证
- 使用装饰器简化权限控制代码
- 支持多租户数据隔离

## 开发规范

### Git 提交规范
```bash
# 提交格式
<type>(<scope>): <subject>

# 类型说明
feat:     新功能
fix:      修复问题
docs:     文档更新
style:    代码格式调整
refactor: 代码重构
test:     测试相关
chore:    构建过程或辅助工具的变动

# 示例
git commit -m "feat(lowcode): 实现实体关系可视化设计器"
git commit -m "fix(backend): 修复用户权限验证问题"
```

### 测试规范
- 单元测试覆盖率不低于 80%
- 集成测试覆盖核心业务流程
- 使用 Jest 进行测试
- 测试文件命名: *.spec.ts

### 文档规范
- API 文档使用 Swagger 自动生成
- 业务文档使用 Markdown 格式
- 代码注释使用中文
- 重要功能必须提供使用示例

## 性能优化规范

### 前端优化
- 使用路由懒加载
- 组件按需导入
- 图片资源优化
- 使用 Vite 的代码分割

### 后端优化
- 数据库查询优化
- 使用 Redis 缓存热点数据
- 实现分页查询
- 使用连接池管理数据库连接

### 数据库优化
- 合理设计索引
- 避免 N+1 查询问题
- 使用事务保证数据一致性
- 定期清理历史数据

## 安全规范

### 认证授权
- 使用 JWT 进行身份认证
- 实现令牌刷新机制
- 使用 Casbin 进行权限控制
- 支持多租户数据隔离

### 数据安全
- 敏感数据加密存储
- 使用 HTTPS 传输
- 实现 CORS 跨域控制
- 输入数据验证和过滤

### 日志安全
- 记录详细的操作日志
- 敏感信息脱敏处理
- 日志文件权限控制
- 定期清理过期日志

## 部署规范

### Docker 规范
- 使用多阶段构建优化镜像大小
- 设置合理的资源限制
- 实现健康检查
- 使用非 root 用户运行

### 环境配置
- 使用环境变量管理配置
- 不同环境使用不同配置文件
- 敏感信息使用密钥管理
- 实现配置热更新

## 监控规范

### 日志监控
- 使用 Winston 进行日志管理
- 实现日志轮转
- 结构化日志输出
- 错误日志告警

### 性能监控
- 监控 API 响应时间
- 监控数据库查询性能
- 监控内存和CPU使用率
- 实现健康检查接口

## 低代码平台规范

### 实体设计
- 实体名使用中文，代码使用英文
- 字段类型必须明确指定
- 关系设计必须考虑性能影响
- 支持实体版本管理

### 代码生成
- 使用 Handlebars 模板引擎
- 模板必须支持变量替换
- 生成的代码必须符合项目规范
- 支持模板版本管理

### API 配置
- API 路径遵循 RESTful 规范
- 参数和响应必须定义类型
- 支持权限配置
- 实现 API 测试功能

## 注意事项

1. **服务间通信**: 使用 HTTP API 进行服务间通信，避免直接数据库访问
2. **数据一致性**: 使用事务保证跨服务数据一致性
3. **错误处理**: 实现统一的错误处理和日志记录
4. **性能考虑**: 合理使用缓存，避免重复查询
5. **安全第一**: 所有用户输入必须验证，敏感操作必须授权
6. **文档完整**: 重要功能必须提供完整文档和使用示例
7. **测试覆盖**: 核心功能必须有完整的测试覆盖
8. **代码质量**: 遵循代码规范，保持代码整洁和可维护性

## 常用命令

### 开发环境启动
```bash
# 一键启动所有服务
./start-dev.sh

# 单独启动服务
pnpm --filter frontend dev
pnpm --filter backend start:dev
pnpm --filter lowcode-platform-backend start:dev
```

### 数据库操作
```bash
# 生成 Prisma 客户端
pnpm prisma:generate

# 数据库迁移
pnpm prisma:migrate

# 查看数据库
pnpm prisma:studio
```

### 测试命令
```bash
# 运行所有测试
pnpm test

# 运行特定服务测试
pnpm --filter backend test
pnpm --filter lowcode-platform-backend test
```

记住：始终遵循项目的架构设计原则，保持代码的一致性和可维护性，确保新功能与现有系统完美集成。